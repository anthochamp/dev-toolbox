name: Publish a dart lib project

on:
  workflow_call:
    inputs:
      checkout-ref:
        description: The branch, tag or SHA to checkout. Defaults to the reference or SHA for that event.
        required: false
        type: string
      package-dir:
        description: Directory within the repository where the package is located
        required: false
        type: string
      dry-run:
        description: "Perform a dry-run only (dart pub publish --dry-run); does not publish to pub.dev"
        required: false
        type: boolean
        default: false
    outputs:
      version:
        description: "Package version read from pubspec.yaml (e.g., 1.2.3)"
        value: ${{ jobs.dart-publish.outputs.version }}
      tag:
        description: "Git tag derived from the package version (e.g., v1.2.3)"
        value: ${{ jobs.dart-publish.outputs.tag }}

jobs:
  dart-publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # required for requesting the JWT
      contents: read
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ inputs.checkout-ref }}

      - uses: dart-lang/setup-dart@e51d8e571e22473a2ddebf0ef8a2123f0ab2c02c # v1.7.1

      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Read package version
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | awk '{print $2}')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"
        working-directory: ${{ inputs.package-dir || '.' }}

      - name: Install dependencies
        run: dart pub get
        working-directory: ${{ inputs.package-dir }}

      - name: Publish (dry run)
        run: dart pub publish --dry-run
        working-directory: ${{ inputs.package-dir }}

      - name: Publish to pub.dev
        if: ${{ !inputs.dry-run }}
        run: dart pub publish -f
        working-directory: ${{ inputs.package-dir }}

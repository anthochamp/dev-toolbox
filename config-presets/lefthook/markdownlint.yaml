pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "*.md"
      run: docker --context default run --rm -v $(pwd):/workdir:z davidanson/markdownlint-cli2:v0.20.0 --no-globs {staged_files}

pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "*.md"
      stage_fixed: true
      run: docker --context default run --rm -v $(pwd):/workdir:z davidanson/markdownlint-cli2:v0.20.0 --fix --no-globs {staged_files}

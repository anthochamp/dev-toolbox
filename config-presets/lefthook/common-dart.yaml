extends:
  - common-dart-base.yaml

pre-push:
  commands:
    test:
      run: |
        if command -v fvm >/dev/null 2>&1; then
          _dart() { fvm dart "$@"; }
        else
          _dart() { dart "$@"; }
        fi
        if ls -A test &>/dev/null; then
          _dart test
        fi
